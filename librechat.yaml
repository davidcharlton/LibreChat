version: "1.2.21"

interface:
  endpointsMenu: true
  modelSelect: true
  agents: true

modelSpecs:
  enforce: false
  prioritize: false
  list: [] # Required explicitly by LibreChat schema

endpoints:
  custom:
    - name: "ZionVM"
      apiKey: "dummy"
      baseURL: "http://10.0.2.5:8000/v1"
      models:
        fetch: true
        default: ["avaniel"] # MUST be an array, not a string
      titleConvo: true
      modelDisplayLabel: "ZionVM"
